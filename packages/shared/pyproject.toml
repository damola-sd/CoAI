[project]
name = "coai-shared"
version = "0.1.0"
description = "Shared utilities for codebase-onboarding-ai (scaffold)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/coai_shared"]

