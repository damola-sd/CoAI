[project]
name = "coai-ingestion"
version = "0.1.0"
description = "Repo ingestion primitives (parsing/chunking/embeddings) (scaffold)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/coai_ingestion"]

