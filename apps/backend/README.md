# Backend (FastAPI)

Scaffold-only FastAPI service managed with `uv`.

## Run locally

```bash
uv sync
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

## Environment

Copy `.env.example` to `.env` and adjust as needed.

